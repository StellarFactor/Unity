namespace StellarFactor.Global
{
    public enum SimpleState { DEFAULT, CURRENT, BUFFER };

    public enum Difficulty { EASY, MEDIUM, HARD };
    public enum QuestionType { GCF, GROUPING, TRINOMIAL };
}
